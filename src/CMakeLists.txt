project(initial_matrix)
cmake_minimum_required(VERSION 3.2)

message(STATUS "!!!!!!!!!!! $[PROJECT_SORCE_DIR]")

include_directories (${PROJECT_SOURCE_DIR}/igg_image)

#add_subdirectory (${PROJECT_SOURCE_DIR}/MyString)

